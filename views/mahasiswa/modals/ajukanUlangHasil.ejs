<div class="modal fade" id="ajukanUlang" tabindex="-1" role="dialog" aria-labelledby="ajukanUlangLabel" 
aria-hidden="true">
<%- include('../../layouts/navbarTopMahasiswa', { id, username }) %>
        <!-- Proposal Form -->
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
              <form action="/mahasiswa/hasil-skripsi/edit" method="post" enctype="multipart/form-data">
                <div class="modal-header">
                    <h4>Ajukan Ulang Bimbingan Hasil</h4>
                </div>
                <div class="modal-body container">
                  <div class="row">
                    <div class="col">
                      <div class="form-group">
                        <label for="npm">NPM</label>
                        <input class="form-control" type="number" name="npm" id="npm" value="<%= profile.npm %>" disabled>
                      </div>
                      <div class="form-group">
                        <label for="nama">Nama</label>
                        <input class="form-control" type="text" name="nama" id="nama" value="<%= profile.nama %>" disabled>
                      </div>
                      <div class="form-group">
                        <label for="hasilSkripsi">Bimbingan Hasil Baru</label>
                        <input class="form-control-file" type="file" accept="*/*" name="hasilSkripsi" id="hasilSkripsi" required>
                      </div>
                      <div class="form-group">
                        <label for="tanggalPengajuan">Tanggal Pengajuan</label>
                        <input class="form-control" type="datetime-local" name="tanggalPengajuan" id="tanggalPengajuan" value="<%= hasilSkripsi.tanggalPengajuan ? new Date(hasilSkripsi.tanggalPengajuan).toLocaleString('sv', { timeZone: 'Asia/Jakarta' }).slice(0, 16) : '' %>" required>
                      </div>                      
                    </div>
                  </div>
                </div>
                <div style="color: red; margin-left: 25px;">
                  Dengan mengajukan ulang bimbingan hasil, 
                  maka bimbingan hasil sebelumnya akan dihapus. 
                  Selanjutnya, Anda dapat mengupload ulang file hasil skripsi yang baru.
                  Apakah Anda ingin mengajukan ulang bimbingan hasil?
                </div>                
                <div class="modal-footer">
                  <button type="submit" class="btn" style="background-color: #325662; color: #FFF;">Ajukan Ulang Bimbingan Hasil</button>
                  <button type="button" class="btn btn-danger" data-dismiss="modal">Batal</button>
                </div>
              </form>
            </div>
          </div>
        </div>
      <!-- /.container-fluid -->
