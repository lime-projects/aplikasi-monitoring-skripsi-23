<div class="modal fade" id="revisiPb2" tabindex="-1" role="dialog" aria-labelledby="revisiPb2Label" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <form action="/mahasiswa/hasil-skripsi-revisi2" method="post" enctype="multipart/form-data">
                <div class="modal-header">
                    <h4>Upload Revisi Bimbingan Hasil </h4>
                </div>
                <div class="modal-body container">
                    <div class="row">
                        <div class="col">
                            <div class="form-group">
                              <label for="hasilSkripsiRevisi2">Revisi Bimbingan Hasil dengan Dosen pembimbing 2</label>
                                <% if (hasilSkripsi.hasilSkripsiRevisi2) { %>
                                    <input class="form-control" type="text" id="hasilSkripsiRevisi2" value="<%= hasilSkripsi.hasilSkripsiRevisi2 %>" disabled>
                                    <br style="margin-top: 10px;">
                                    <a href="/mahasiswa/hasil-skripsi-revisi/download/<%= hasilSkripsi.id %>" download="<%= hasilSkripsi.hasilSkripsiRevisi2.split('/').pop() %>" class="btn btn-primary">Unduh Revisi Hasil Skripsi</a>
                                    <br><br>
                                    <input class="form-control-file" type="file" accept="*/*" 
                                    name="hasilSkripsiRevisi2" id="hasilSkripsiRevisi2" required>
                                  <% } else { %>
                                    <input class="form-control-file" type="file" accept="*/*" name="hasilSkripsiRevisi2" id="hasilSkripsiRevisi2" required>
                                  <% } %>
                                </div>
                              <div class="form-group">
                                <label for="tanggalRevisiMahasiswa2">Tanggal Mahasiswa Revisi</label>
                                <input class="form-control" type="datetime-local" name="tanggalRevisiMahasiswa2" id="tanggalRevisiMahasiswa2" value="<%= hasilSkripsi.tanggalRevisiMahasiswa2 ? new Date(hasilSkripsi.tanggalRevisiMahasiswa2).toLocaleString('sv', { timeZone: 'Asia/Jakarta' }).slice(0, 16) : '' %>" required>
                        </div>
                    </div>
                </div>
                <div style="color: red; margin-left: 5px;">Anda dapat mengupload hasil skripsi yang telah diperbaharui.</div>
                <div class="modal-footer">
                    <button type="submit" class="btn" style="background-color: #325662; color: #FFF;">Kirim</button>
                    <button type="button" class="btn btn-danger" data-dismiss="modal">Batal</button>
                </div>
                </div>
            </form>
        </div>
    </div>
  </div>
  